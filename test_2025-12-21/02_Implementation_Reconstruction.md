# Phase 2: First-Principles Implementation (原理実装)
> **Action:** 「作れなければ理解したことにならない」．C言語の構造体，あるいは擬似コードで定義せよ．

## 1. The Struct (構造体定義)
* この概念を  で表現するなら？
```c
typedef struct {
    // 必要なメンバ変数は？
    // double energy;
    // complex double state;
} test_t;
```

## 2. The Algorithm (アルゴリズム)
* 入力を出力に変換する  を書け．(詳細な文法よりロジック優先)
```c
void update(test_t *sys) {
    // ここに物理法則/ロジックを記述
}
```

## 3. Lecture Note (他者への説明)
* 未来の自分に向けて，この実装の「肝」を一言で講義せよ．
    *
