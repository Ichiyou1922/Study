# Phase 1: Abstraction & Geometry (構造化)
> **Query:** 現象を「数学的な対象」または「幾何学的な対象」として再定義せよ．

## 1. Isomorphism (同型写像)
- どの抽象枠に写像すると操作規則が見えるか．最も近いものにチェック．(複数可)
    - [ ] 状態空間モデル（状態ベクトル＋更新写像）
    - [ ] グラフ／ネットワーク（ノード＋エッジ＋局所規則）
    - [ ] 確率過程／ベイズ推論（分布＋遷移＋観測）
    - [ ] 最適化（目的関数＋制約＋勾配／探索）
    - [ ] 幾何／位相（多様体，曲率，不変量，ホモトピー）
    - [ ] 代数（群，環，線形空間，作用，表現）
    - [ ] 計算モデル（オートマトン，プログラム意味論，計算量）
    - [ ] その他: ________________

## 1.5 Frame Commitment (枠の固定)
- 上のチェックから「主枠」を1つだけ選び，以下を各1行で確定せよ．(後で変更可だが，変更理由を明記せよ)
    - 主枠: 
    - 状態 (State): 
    - 更新 (Update): 
    - 観測 (Observation): 

## 2. Variables & Dynamics (変数と力学)
- システムを構成する最小単位 ($e$) を定義せよ．
- 時間発展 ($t \to t+1$) の更新規則を記述せよ．(例: $H e = E e$)
    - $S = \{ ... \}$
    - Rule:

## 3. Visualization (脳内レンダリング)
- 目を閉じて想像した時，何が「保存」され，何が「変形」され，何が「散逸」するか．各1つずつ書け．
    - 保存:
    - 変形:
    - 散逸:
